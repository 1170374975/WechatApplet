<view class="container">
  <view class="page-section">
    <view>图片上传，且上传到云存储，上传到云数据</view>
    <button bindtap="upload" type="primary">上传图片</button>
    <block wx:for="{{imgUrlArr}}">
      <image src="{{item}}" wx:if="{{showImg}}"></image>
    </block>
    
  </view>
</view>
